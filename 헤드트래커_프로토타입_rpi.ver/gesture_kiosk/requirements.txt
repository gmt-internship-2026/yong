# 추론(라즈베리파이5, aarch64 리눅스, Python 3.11) 고정 버전 (설치가이드.md 참고)
#
# 2026-07-22 win.ver(윈도우)에서 이식. mediapipe·opencv-python은 aarch64 리눅스 휠
#   존재 여부를 이 세션(윈도우 개발 PC)에서 실제로 확인하지 못했다 — install.sh 실행 중
#   `pip install`이 해당 버전을 못 찾으면(빌드 시도로 멈추면) 버전 핀을 살짝 낮추거나
#   `mediapipe>=0.10,<0.11` 처럼 범위로 풀어서 재시도할 것.
# pyttsx3는 리눅스에서 pywin32를 요구하지 않는다(그 의존성 자체가 win.ver에서도
#   requirements.txt에 없고, pyttsx3 자체 platform 마커로 윈도우에서만 pywin32를 끌어옴) —
#   대신 시스템 패키지 espeak-ng가 필요하다(pip 아님, install.sh에서 apt로 설치).
mediapipe==0.10.35                 # 얼굴 랜드마크 + 블렌드셰이프 (Apache-2.0)
opencv-python==5.0.0.93
numpy==2.4.6                       # mediapipe 0.10.35는 numpy 2.x와 호환(win.ver 실측)
pyyaml==6.0.3
fastapi==0.139.0
uvicorn==0.51.0
psutil==7.2.2
pyttsx3==2.99                      # 음성 안내 TTS — 리눅스는 espeak-ng 드라이버로 동작
# picamera2는 pip가 아니라 apt(python3-picamera2)로 설치한다 — libcamera 바인딩이
#   시스템 패키지라 pip 휠로 배포되지 않는다. install.sh가 --system-site-packages venv를
#   만드는 이유이기도 하다. camera.backend: v4l2(기본, USB 웹캠)면 이 줄 자체가 불필요.
