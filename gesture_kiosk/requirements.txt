# 추론(윈도우 + NVIDIA + Python 3.11.5) 고정 버전 — 타 PC 재현성 보장 (설치가이드.md 참고)
#
# torch / torchvision은 CUDA 12.8 휠을 별도 인덱스로 설치해야 해 여기 없다 (install.bat이 처리):
#   pip install torch==2.11.0+cu128 torchvision==0.26.0+cu128 --index-url https://download.pytorch.org/whl/cu128
# tensorrt(선택, 엔진 백엔드)도 install.bat /engine 단계에서 설치한다.
#
# 개발 맥(Apple Silicon)은 torch/torchvision을 기본 인덱스로 설치하면 된다 (버전 무관 동작).
ultralytics==8.4.91
opencv-python==5.0.0.93
opencv-python-headless==5.0.0.93   # easyocr 의존성 — opencv-python과 반드시 같은 버전 (cv2 충돌 방지)
numpy==2.2.6
pyyaml==6.0.3
fastapi==0.139.0
uvicorn==0.51.0
psutil==7.2.2
easyocr==1.7.2                     # 주민등록증 OCR (ocr.enabled=false면 미사용)
pyttsx3==2.99                      # 음성 안내 TTS — 윈도우 SAPI (announce.backend=tts)
